pre-commit:
  commands:
    oxlint:
      run: pnpm exec oxlint . --quiet
    oxfmt:
      run: pnpm exec oxfmt . --check

commit-msg:
  commands:
    commitlint:
      run: pnpm exec commitlint --edit {1}
