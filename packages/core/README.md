# gits Core

Reusable services and contracts behind the `gits` command-line interface.

Replace `your-scope` with the scope for the chosen release channel:

```sh
npm install @your-scope/gits-core
```

The package is published as Node.js ESM and requires Node.js 22.17.1 or newer.
