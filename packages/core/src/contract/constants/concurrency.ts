export enum ConcurrentRunStatus {
  Fulfilled = 'fulfilled',
  Rejected = 'rejected',
  NotRun = 'not-run',
}

export enum ConcurrentTaskOutcomeStatus {
  Completed = 'completed',
  Failed = 'failed',
  Skipped = 'skipped',
}
